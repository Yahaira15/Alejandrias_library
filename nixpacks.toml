[variables]
NIXPACKS_NODE_VERSION = "22.12.0"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd alejandrias_frontend && npm ci"]

[phases.build]
cmds = ["cd alejandrias_frontend && npm run build"]

[start]
cmd = "cd alejandrias_frontend && npm start"
