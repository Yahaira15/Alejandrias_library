{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "/opt/venv/bin/python -m pip install --upgrade pip && /opt/venv/bin/python -m pip install -r requirements.txt && /opt/venv/bin/python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "/opt/venv/bin/python -m gunicorn service_ia.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --access-logfile - --error-logfile -",
    "healthcheckPath": "/api/ia/",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
